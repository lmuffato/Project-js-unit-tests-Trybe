const calculator = {
  add: (num, num2) => num + num2,
  mult: (num, num2) => num * num2,
  div: (num, num2) => Math.floor(num / num2),
  sub: (num, num2) => num - num2,
};

module.exports = calculator;
